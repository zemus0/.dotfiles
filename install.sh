# yay -S --needed installed.txt
# sudo systemctl enable lightdm
#
# git clone git@github.com:zemaus/.dotfiles.git $HOME/
# git clone https://github.com/tmux-plugins/tpm $HOME/.tmux/plugins/tpm
# sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
# git clone https://github.com/zsh-users/zsh-syntax-highlighting.git ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-syntax-highlighting
#
# pywalfox install
# sudo chmod +x /usr/lib/python3.11/site-packages/pywalfox/bin/main.sh
#
# ln -sf "$HOME/.dotfiles/others/Xmodmap" "$HOME/.Xmodmap"
# ln -sf "$HOME/.dotfiles/zsh/zshrc" "$HOME/.zshrc"
# touch "$HOME/.dmenu_history"
# ln -sf "$HOME"/.dotfiles/config/* "$HOME/.config/"
# sudo ln -sf "$HOME/.dotfiles/slick-greeter.conf" /etc/lightdm/
# sudo sed -i 's/#greeter-session=.*/greeter-session=lightdm-slick-greeter/g' /etc/lightdm/lightdm.conf
# sudo mkdir /usr/share/wallpapers 
# sudo chmod 777 /usr/share/wallpapers
#
# git config --global mergetool.fugitive.cmd 'nvim -f -c "Gvdiffsplit!" "$MERGED"'
# git config --global merge.tool fugitive
